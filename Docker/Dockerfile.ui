FROM python:3.10

WORKDIR /app

ENV PYTHONUNBUFFERED=1

RUN pip install streamlit==1.57.0 requests==2.34.2

COPY ui.py ./ui.py

EXPOSE 8501

CMD ["streamlit", "run", "ui.py", "--server.address", "0.0.0.0", "--server.port", "8501"]
